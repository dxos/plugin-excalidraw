---
'@dxos/plugin-tictactoe': minor
---

Migrate to the current DXOS SDK: Effect 4 schema APIs, `Capability.lazyModule`/`contribute`, the `AppCapability` module makers, and the `@dxos/plugin-game` namespace subpaths.
